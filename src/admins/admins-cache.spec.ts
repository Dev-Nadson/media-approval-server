import { Cache } from "cache-manager"

// Verificar se consigo puxar o cache do list-users
// Verificar se consigo puxar o cache do get-users

// Verificar se consigo invalidar o cache do get-users

// Verificar se consigo invalidar o cache do list-users

describe('AdminsCache (Unit Test)', () => {
    let cacheManager: Cache

    beforeEach(() => { })

    it('should return the cache of list-users', () => { })
    it('should return the cache of get-users', () => { })
    it('should invalidate the cache of get-users', () => { })
    it('should invalidate the cache of list-users', () => { })
})